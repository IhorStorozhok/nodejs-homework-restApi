const ctrlWrapper = require("./ctrlWrapper");
const validation = require("./contactsValidation");
const registerValidation = require("./registerValidation");
const auth = require("./auth");
const upload = require("./upload");

module.exports = {
  ctrlWrapper,
  validation,
  registerValidation,
  auth,
  upload,
};
